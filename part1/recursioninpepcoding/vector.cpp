#include<iostream>
#include<algorithm>
#include<vector>
using namespace std;
int main(){ 
    vector <int > v ;
    v.push_back(ck(20);
    v.push_back(30);
    v.push_back(40);
    v.push_back(50);
    // v.resize(5);

    // cout<<v.size()<<endl;
    // v.pop_back();
    // // v.clear();
    // cout<<v.size()<<endl;
    // cout<<"is it zero--->"<<v.empty();
    // v.erase(v.beg10);
    // v.push_bain()+1);
for (int i = 0; i < v.size(); i++)
    {
        cout<<v[i]<<endl;
    }
    v.insert(v.begin()+1,20);
    for (int i = 0; i < v.size(); i++)
    {
        cout<<v[i]<<endl;
    }
    // reverse(v.begin(),v.end());everse.be
    cout<<"reverse"<<endl;
    reverse(v.begin()+4,v.end());


    for (int i = 0; i < v.size(); i++)
    {
        cout<<v[i]<<endl;
    }
    sort(v.begin(),v.end());
    
        return 0;
}